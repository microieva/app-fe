export interface UserInput {
    id?: number
    firstName: string
    lastName: string
    dob: string
    email: string
    phone: string
    streetAddress: string
    city: string
    postCode: string
}